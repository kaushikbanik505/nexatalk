# Same Stream API key as STEAM_API_KEY in backend/.env.example
VITE_STREAM_API_KEY=your_stream_api_key

# Deployed backend's base URL, no trailing slash and no /api suffix
# (e.g. your Render service URL) - only needed in production builds
VITE_API_URL=https://your-backend.onrender.com
